<h1>Links tagged with [<%= params[:tag] %>]</h1>
<p>Showing <%= @links.size %> links</p>

<ul>
	<% @links.each do |link| %>
		<li>
			<%= render 'links/link', link: link %>
		</li>
	<% end %>
</ul>

<%= link_to 'Back to tags list', tags_path, class: 'btn btn-flat' %>